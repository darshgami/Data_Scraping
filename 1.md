Update the existing scraping system with the following fixes:

1. DEBUG DATA BEFORE CSV:
- Before generating the CSV file, log the scraped results.
- Ensure the scraper is returning data.
- If results are empty, do not generate empty CSV silently.

2. FIX CITY FILTER:
- Modify city filtering logic to avoid removing all records.
- Apply filter ONLY if address exists.
- Use case-insensitive partial match.

Correct logic:
- If city is provided:
  - Include record ONLY if address contains the city (case-insensitive)
  - If address is empty, allow it as "N/A"

3. IMPROVE EXTRACTION LOGIC:
- Replace strict selectors with flexible fallback extraction:
  - Extract all visible text blocks
  - Split text by lines
  - First line → Company Name
  - Second line → Address
- Skip empty entries
- Ensure at least 2 lines before extracting

4. HANDLE EMPTY DATA:
- If no valid records found:
  - Still generate CSV with headers
  - Return message: "No data found for selected city"

5. CSV WRITING FIX:
- Ensure CSV writes actual data using:
  - writeheader()
  - writerows(results)
- Use UTF-8 encoding
- Ensure file is not overwritten before writing

6. VALIDATION RULES:
- If Company Name missing → "N/A"
- If Address missing → "N/A"
- Remove duplicates before writing

7. FINAL CHECK BEFORE DOWNLOAD:
- Log total records count
- If count > 0 → generate CSV
- If count = 0 → still generate file but with only headers

8. DO NOT CHANGE:
- Do not add extra fields
- Keep only:
  - Company Name
  - Address
- Keep direct download flow unchanged